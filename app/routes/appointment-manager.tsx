import { Result } from 'antd';

export default function AppointmentManager() {
  return (
    <Result
      title="Appointment Manager"
      subTitle="Manage appointments, view calendar, and search by customer"
    />
  );
}